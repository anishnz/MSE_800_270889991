"""
Week 5 - Activity 5.1: List Comprehension exercise (placeholder file).

DEFINITION: List Comprehension
-------------------------------
A list comprehension is a compact way to build a list in one line, using
the form:

    [expression for item in iterable if condition]

It replaces the longer pattern of creating an empty list and using a
for-loop with .append() inside it.

Status of this file
--------------------
This script was left empty in the original exercise submission -- no
code was ever written into it. Per the documentation task, no new logic
is being added here (only this explanatory docstring), so the file's
behaviour (doing nothing when run) is unchanged from before.
"""
