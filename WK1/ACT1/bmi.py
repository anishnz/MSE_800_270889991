# ============================================================
# Exercise: BMI Calculator (Activity 1) - Starter File
# ============================================================
#
# This file is the starting point for the "BMI Calculator" activity
# (WK1 / ACT1). The folder and filename ("bmi.py") indicate the intended
# exercise is to build a Body Mass Index (BMI) calculator that:
#   - asks the user for their weight (kg) and height (m),
#   - calculates BMI using the formula: BMI = weight / (height ** 2),
#   - prints the BMI value and/or a category (e.g. underweight, normal,
#     overweight, obese) based on that value.
#
# As currently written, the file only contains a single "Hello, World!"
# smoke-test line. This is a common first step when setting up a new
# Python exercise: confirm that the file runs correctly (e.g. the
# interpreter/environment is configured properly) before writing the
# real program logic on top of it. No BMI calculation logic has been
# implemented yet in this version of the file.
# ============================================================

# print() is a built-in function that writes text to the console/terminal.
# This line simply outputs the text "Hello, World!" to confirm the script
# can be executed successfully. It does not perform any BMI calculation.
print("Hello, World!")