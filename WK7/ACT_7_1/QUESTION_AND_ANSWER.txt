QUESTION
========
Exercise: Bank Account Management System

Problem Statement
A bank wants a simple program to manage customer accounts. Every account
has an account number, a customer name, and a balance, and needs to
support depositing and withdrawing money. Savings accounts are a special
kind of account that should also be able to calculate interest earned on
the current balance.

Task: Use inheritance to model this system so that the savings-account
behaviour builds on top of the general bank-account behaviour instead of
duplicating it.

Requirements
Create the following classes:
    1. BankAccount - stores account_number, customer_name, balance, and
       provides methods to display details, deposit, and withdraw money.
    2. SavingsAccount (inherits from BankAccount) - adds a method to
       calculate interest, and displays "Account Type: Savings Account"
       when showing details.


ANSWER
======
This exercise is modelled using SINGLE INHERITANCE: one child class,
SavingsAccount, inherits from exactly one parent class, BankAccount.

Design:
    1. BankAccount (parent/base class) holds everything common to any
       bank account:
         - __init__(self, account_number, customer_name, balance): sets
           up the three shared attributes on the object.
         - display_account_details(self): prints account number,
           customer name, and balance.
         - deposit(self, amount): validates the amount is positive, then
           increases self.balance and prints a confirmation.
         - withdraw(self, amount): validates the amount is positive and
           does not exceed the balance, then decreases self.balance and
           prints a confirmation (or an error message if invalid).

    2. SavingsAccount(BankAccount) (child/subclass) reuses everything
       above automatically because of inheritance, and adds:
         - calculate_interest(self, interest_rate): computes
           balance * interest_rate / 100, prints it, and returns it.
         - display_account_details(self): OVERRIDES the parent's
           version. It prints "Account Type: Savings Account" first,
           then calls super().display_account_details() to reuse the
           parent class's printing logic for the remaining fields,
           rather than repeating that code.

    3. The demonstration code creates one SavingsAccount (account
       SA1001, customer John, starting balance $5000), then:
         - shows the initial details,
         - deposits $1000,
         - withdraws $500,
         - calculates interest at 5%,
         - shows the final details.

    Because SavingsAccount inherits deposit() and withdraw() unchanged
    from BankAccount, no extra code was needed to support those
    operations on a savings account - this is the main benefit of
    single inheritance: shared behaviour is written once in the parent
    and reused by the child, while the child is still free to add or
    override behaviour specific to itself.

Output when run:
    Initial Account Details
    Account Type: Savings Account
    Account Number: SA1001
    Customer Name: John
    Account Balance: $5000.00

    After Deposit
    Deposited: $1000.00
    New Balance: $6000.00

    After Withdrawal
    Withdrawn: $500.00
    New Balance: $5500.00

    Interest Calculation
    Interest at 5.00%: $275.00

    Final Account Details
    Account Type: Savings Account
    Account Number: SA1001
    Customer Name: John
    Account Balance: $5500.00


MORE QUESTIONS AND ANSWERS (FOR BETTER UNDERSTANDING)
=====================================================

Q1. What is inheritance, in simple words?
A1. Inheritance lets a new class (the CHILD) reuse everything from an
    existing class (the PARENT) and then add or change only what is
    different. It models an "is-a" relationship: a SavingsAccount IS A
    BankAccount, so it should do everything a BankAccount does.

Q2. Which class is the parent and which is the child here?
A2. BankAccount is the parent (base class). SavingsAccount is the child
    (subclass). In code the parent goes in brackets:
        class SavingsAccount(BankAccount):

Q3. What does super() mean in display_account_details()?
A3. super() means "the parent class". super().display_account_details()
    runs the parent's version of the method. SavingsAccount first
    prints "Account Type: Savings Account" and then calls the parent to
    print the number, name and balance, so the printing code is not
    written twice.

Q4. What is method overriding?
A4. When the child defines a method with the SAME NAME as the parent's,
    Python uses the child's version for child objects.
    SavingsAccount overrides display_account_details() to add the
    account-type line. deposit() and withdraw() are NOT overridden, so
    the parent's versions are used.

Q5. How is the interest calculated? Show the numbers.
A5. interest = balance * interest_rate / 100
    After the deposit and withdrawal the balance is 5500, so at 5%:
        5500 * 5 / 100 = 275.00

Q6. What happens if someone withdraws more than the balance, or a
    negative amount?
A6. withdraw() validates first. If the amount is not positive, or is
    greater than the balance, it prints an error message and does NOT
    change the balance. The balance never goes below zero.

Q7. Can a plain BankAccount call calculate_interest()?
A7. No. Inheritance only goes DOWN: the child gets the parent's methods,
    but the parent knows nothing about methods that only the child has.
    Calling calculate_interest() on a BankAccount raises AttributeError.

Q8. How can we prove a SavingsAccount is also a BankAccount?
A8. isinstance(account, BankAccount) returns True for a SavingsAccount
    object, because a child object counts as an instance of its parent.

Q9. What is the main benefit of using inheritance here?
A9. Write once, reuse everywhere. Deposit and withdraw are written only
    in BankAccount. If a new type is needed later (e.g. CurrentAccount
    with an overdraft), it can inherit the same code instead of copying
    it.
